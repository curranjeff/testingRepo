def verify_Time(timeYears:int):
    """helper function to verify years are valid

    Args:
        timeYears (int): the year to verify as valid

    Returns:
       bool: true if valid, else false
    """
    return timeYears >= 0